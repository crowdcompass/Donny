#include "Pods-StateMachine.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/StateMachine" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/StateMachine"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}